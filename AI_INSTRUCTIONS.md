# 📋 إرشادات التطوير - AI Development Guidelines

هذا الملف يحتوي على إرشادات مهمة يجب اتباعها عند بناء أي ميزة جديدة في المشروع.

---

## 🎨 الأيقونات (Icons)

### ✅ **يجب استخدام:**
- **icomoon.io** - الأيقونات المجانية (Free Icons)
- تأكد أن الأيقونة **free** وليست premium
- استخدم Font Icons من icomoon أو SVG

### ❌ **لا تستخدم:**
- Font Awesome (ما لم يكن موجود مسبقاً)
- أي مكتبة أيقونات مدفوعة
- أيقونات من مصادر غير معتمدة

### 📝 **مثال:**
```html
<!-- ✅ صحيح -->
<span class="icon-home"></span>
<!-- من icomoon free icons -->

<!-- ❌ خطأ -->
<i class="fa fa-home"></i>
<!-- Font Awesome -->
```

---

## 🎨 الألوان والمتغيرات

### ✅ **يجب استخدام:**
- CSS Variables من `:root` في `layout.jinja`
- المتغيرات المحددة مسبقاً في المشروع
- نفس أسماء المتغيرات للدارك مود

### 📝 **مثال:**
```css
/* ✅ صحيح */
.my-element {
  background-color: var(--card-background-color);
  color: var(--card-title-color);
}

/* ❌ خطأ */
.my-element {
  background-color: #ffffff;
  color: #2D2424;
}
```

---

## 🌓 الدارك مود (Dark Mode)

### ✅ **يجب اتباع:**
- استخدام `[data-theme="dark"]` للتحكم اليدوي
- استخدام `@media (prefers-color-scheme: dark)` للوضع التلقائي
- نفس أسماء المتغيرات في الوضعين
- استخدام `:root:not([data-theme])` لتجنب التعارض

### 📝 **مثال:**
```css
:root {
  --my-color: #ffffff;
}

[data-theme="dark"] {
  --my-color: #1a1a1a;
}

@media (prefers-color-scheme: dark) {
  :root:not([data-theme]) {
    --my-color: #1a1a1a;
  }
}
```

---

## 📁 هيكل الملفات

### ✅ **يجب اتباع:**
- استخدام `.jinja` للملفات القوالب
- استخدام `.scss` للـ styles (يتم تحويلها لـ CSS)
- وضع المكونات في `components/`
- وضع الأقسام في `sections/`

### 📝 **مثال:**
```
components/
  ├── product-card.jinja
  ├── header.jinja
sections/
  ├── products-section.jinja
  ├── gallery-sec.jinja
assets/
  ├── style.scss
  ├── style.css
```

---

## 🎯 JavaScript

### ✅ **يجب اتباع:**
- استخدام vanilla JavaScript (بدون مكتبات إضافية)
- تجنب jQuery ما لم يكن موجود مسبقاً
- حفظ التفضيلات في `localStorage` عند الحاجة

### 📝 **مثال:**
```javascript
// ✅ صحيح - vanilla JS
document.body.setAttribute('data-theme', 'dark');
localStorage.setItem('theme', 'dark');

// ❌ خطأ - jQuery (ما لم يكن موجود)
$('body').attr('data-theme', 'dark');
```

---

## 📱 Responsive Design

### ✅ **يجب اتباع:**
- استخدام CSS Media Queries
- Mobile First Approach عند الإمكان
- اختبار على أحجام مختلفة

### 📝 **مثال:**
```scss
// Mobile First
.my-element {
  padding: 10px;
  
  @media (min-width: 768px) {
    padding: 20px;
  }
  
  @media (min-width: 1200px) {
    padding: 30px;
  }
}
```

---

## 🔤 الخطوط (Fonts)

### ✅ **يجب اتباع:**
- استخدام الخط المحدد في `settings.fonts_name`
- الخط الافتراضي: 'Changa'
- استخدام Google Fonts

### 📝 **مثال:**
```css
font-family: {% if settings.fonts_name %}{{ settings.fonts_name }}{% else %}'Changa'{% endif %}, sans-serif;
```

---

## 🎨 التصميم

### ✅ **يجب اتباع:**
- استخدام Border Radius: `8px`, `12px` للعناصر
- استخدام Box Shadow للعمق
- استخدام Transitions للحركات السلسة
- استخدام الألوان من المتغيرات

### 📝 **مثال:**
```scss
.card {
  border-radius: 12px;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  
  &:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
  }
}
```

---

## 🔧 الإعدادات (Settings)

### ✅ **يجب اتباع:**
- استخدام `settings` من `layout.jinja`
- التحقق من وجود الإعداد قبل الاستخدام
- استخدام قيم افتراضية عند عدم وجود الإعداد

### 📝 **مثال:**
```jinja
{% if settings.general_productcard_background %}
  --card-background-color: {{ settings.general_productcard_background }};
{% else %}
  --card-background-color: #ffffff;
{% endif %}
```

---

## 📦 المكتبات المسموحة

### ✅ **مسموح:**
- Bootstrap (إذا كان موجود مسبقاً)
- jQuery (إذا كان موجود مسبقاً)
- Slick Slider (إذا كان موجود مسبقاً)

### ❌ **غير مسموح:**
- إضافة مكتبات جديدة بدون موافقة
- مكتبات مدفوعة
- مكتبات ثقيلة بدون حاجة

---

## 🧪 الاختبار

### ✅ **يجب:**
- اختبار الدارك مود واللايت مود
- اختبار Responsive على أحجام مختلفة
- اختبار على متصفحات مختلفة
- التحقق من عدم وجود أخطاء في Console

---

## 📝 التوثيق

### ✅ **يجب:**
- كتابة تعليقات واضحة في الكود
- توثيق المتغيرات الجديدة
- توثيق الميزات الجديدة

### 📝 **مثال:**
```scss
/* 
 * متغيرات كارد المنتج
 * تستخدم في product-card.jinja
 */
:root {
  --card-background-color: #ffffff;
  --card-title-color: #2D2424;
}
```

---

## ⚠️ تحذيرات مهمة

1. **لا تحذف** كود موجود بدون استشارة
2. **لا تغير** أسماء المتغيرات الموجودة
3. **لا تضيف** مكتبات جديدة بدون موافقة
4. **تأكد** من التوافق مع الدارك مود
5. **استخدم** نفس نمط الكود الموجود

---

## 🎯 قائمة سريعة للتحقق

قبل إرسال أي كود، تأكد من:

- [ ] استخدام الأيقونات من icomoon (free)
- [ ] استخدام CSS Variables
- [ ] دعم الدارك مود
- [ ] Responsive Design
- [ ] لا مكتبات جديدة
- [ ] استخدام الإعدادات من settings
- [ ] تعليقات واضحة
- [ ] اختبار على متصفحات مختلفة

---

## 📞 ملاحظات إضافية

- **راجع** هذا الملف قبل بناء أي ميزة جديدة
- **اتبع** نفس نمط الكود الموجود
- **اسأل** إذا كان هناك أي شك

---

**آخر تحديث:** 2024

